[package]
name = "clowder-config"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { workspace = true }
toml = "0.8"
serde_json = "1"
rustix = { version = "0.38", features = ["fs"] }

[dev-dependencies]
tempfile = "3"
