---
version: '0.5.0'
date: '2026-08-07'
---

- Agent worktrees now live outside your project entirely, under a per-user data directory, instead
  of inside a `.clowder/` folder in your repo. Nothing extra shows up in `git status` or your
  editor's file tree, and worktrees you already had keep working right where they are.
